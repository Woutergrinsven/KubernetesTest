apiVersion: v1
kind: Service
metadata:
  name: consoleapp1-service
spec:
  type: NodePort
  selector:
    app: consoleapp1-pod-label
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080 #host machine port